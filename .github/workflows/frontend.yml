    name: Frontend
    on: [push]
    jobs:
      build:
        runs-on: ubuntu-latest
        
        steps:
        - uses: actions/checkout@v1
        - name: Install yarn
          run: cd frontend
          run: yarn install
        - name: Build app
          run: cd frontend
          run: yarn run build
